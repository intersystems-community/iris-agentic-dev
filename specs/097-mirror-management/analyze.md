# Specification Analysis Report — 097-mirror-management

**Date**: 2026-09-02
**Analyzed**: `spec.md`, `plan.md`, `tasks.md`, `data-model.md`,
`contracts/iris_admin_mirror.md`, `research.md`
**Constitution Version**: 1.3.2

---

## Findings Table

| ID  | Category               | Severity | Location(s)                                                              | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Recommendation                                                                                                                                                                                                                                                                                                              |
| --- | ---------------------- | -------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | Duplication            | MEDIUM   | spec.md FR-013–015, plan.md Test Plan                                    | Test layer definitions appear in full in both `spec.md` (FR-013–015) and `plan.md` Test Plan Summary table. Both are complete, overlapping representations of the same three test layers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Remove the Test Plan Summary table from `plan.md` (or reduce to a cross-reference). Spec is the single source of truth for test requirements.                                                                                                                                                                               |
| A2  | Duplication            | LOW      | spec.md Assumptions, plan.md Notes, tasks.md Notes                       | The `%SYSTEM.Mirror` vs `SYS.Mirror` class distinction is stated four times across spec.md (Assumptions), plan.md (Notes), tasks.md (Notes), and research.md. Reasonable in research.md; redundant elsewhere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Retain in spec.md Assumptions and research.md; trim the identical restatement in plan.md Notes and tasks.md Notes to a `research.md` cross-reference.                                                                                                                                                                       |
| B1  | Ambiguity              | HIGH     | spec.md FR-004                                                           | "caught by pattern-matching the ObjectScript error string" for `MIRROR_VERSION_MISMATCH` — no pattern string is specified. The keyword heuristics (`"version"`, `"incompatible"`) appear only in plan.md implementation notes, not in the spec or data-model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Add the exact match patterns (or explicit list) to `data-model.md` error code table. Behavior that depends on string matching must be deterministic.                                                                                                                                                                        |
| B2  | Ambiguity              | MEDIUM   | spec.md FR-006, contracts                                                | `confirm: true` is described as "an extra guard" but the contract says "not currently enforced but reserved." Neither spec nor plan defines under what condition (if ever) `confirm` becomes required or what happens when it is `false` today.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Either enforce `confirm: true` in v1 or remove the parameter. Unenforced optional guards add API surface without safety value.                                                                                                                                                                                              |
| B3  | Ambiguity              | MEDIUM   | tasks.md T010, T016                                                      | T010 and T016 reference "or a new `test_mirror_binary.rs` in that directory if file is too large" — the choice of target file is deferred to implementation time with no decision criterion.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Decide the file name at task-writing time. The uncertainty will cause an inconsistency between T010 and T017 if they end up in different files.                                                                                                                                                                             |
| B4  | Ambiguity              | LOW      | research.md (JoinMirrorAsAsyncMember notes)                              | "`InstanceName`: the IRIS instance name of the _failover member_ to join to" — the annotation says "failover member" but this is the async candidate joining a set whose primary is a failover member. The directional language is confusing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Reword to "the IRIS instance name on the primary failover member being joined." Does not block implementation but will confuse future readers.                                                                                                                                                                              |
| C1  | Underspecification     | HIGH     | spec.md US1 AC3, FR-004                                                  | Version mismatch detection: spec says errors are "caught by pattern-matching the ObjectScript error string." No test asserts this path. FR-013 unit tests cover only missing-param paths; FR-014 binary tests only test gate enforcement. There is no test that feeds a version-mismatch error string and asserts `MIRROR_VERSION_MISMATCH` error code.                                                                                                                                                                                                                                                                                                                                                                         | Add a unit test that constructs the error-string match path (inject a mock output string like `"ERROR:version incompatible"`) and asserts `error_code: "MIRROR_VERSION_MISMATCH"`. US1 AC3 has no covering task.                                                                                                            |
| C2  | Underspecification     | HIGH     | spec.md US1 AC5 (SSL error path)                                         | SSL error surface ("error explains SSL is required and what parameters to provide") has no corresponding task or test. FR-013–015 do not cover SSL validation or SSL error messaging. The `ssl_enabled` flag is accepted but its validation path (and error output) is untested.                                                                                                                                                                                                                                                                                                                                                                                                                                                | Add a unit test for the SSL error path or explicitly mark AC5 as post-MVP and document the deferral in spec.md Out of Scope. Currently it is an acceptance criterion with zero coverage.                                                                                                                                    |
| C3  | Underspecification     | MEDIUM   | data-model.md MirrorAddAsyncResult                                       | One error variant in `data-model.md` is missing an `error_code` field: `{ "success": false, "error": "BecomePrimary returned false" }` — this is under `MirrorAddAsyncResult` but clearly belongs to `MirrorFailoverResult`. Regardless, neither error variant has an `error_code`, making them inconsistent with all other variants.                                                                                                                                                                                                                                                                                                                                                                                           | Move the `BecomePrimary` error to `MirrorFailoverResult` only. Add `"error_code": "MIRROR_BECOME_PRIMARY_FAILED"` (or similar) so agents can branch on code rather than error string.                                                                                                                                       |
| C4  | Underspecification     | MEDIUM   | spec.md FR-005, tasks.md T012                                            | FR-005 says `mirror_add_async` returns `{ "success": false, "error_code": "ALREADY_MEMBER", "mirror_name": "..." }` where `mirror_name` is the existing set's name. T012 says parse `ALREADY_MEMBER                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | <names>`but the pipe-delimited output provides _the mirror names list_, not a single name. The spec says a single`mirror_name` field; the ObjectScript writes a comma list.                                                                                                                                                 | Clarify whether `mirror_name` is the first element of the list or a stringified list. The data-model and contracts show `"mirror_name": "EXISTINGSET"` (singular) — align the implementation note in T012. |
| C5  | Underspecification     | LOW      | spec.md FR-003, tasks.md                                                 | FR-003 states the `SYS.Mirror` method signature "must be verified against live IRIS before coding" and documented in `research.md`. `research.md` exists and contains the verification. However, no task in tasks.md explicitly closes FR-003 (there is no "verify research.md is complete" checkpoint task). The plan.md constitution check marks II as PASS but the spec still flags it as open.                                                                                                                                                                                                                                                                                                                              | This is a low-risk gap (research.md is complete) but add a Phase 1 read task or checkpoint note that explicitly confirms FR-003 is satisfied by the existing `research.md`.                                                                                                                                                 |
| D1  | Constitution Conflict  | CRITICAL | tasks.md T016, constitution IV                                           | T016 tests `iris_mirror_failover_impl` with `iris=None` and asserts `IRIS_UNREACHABLE`. Constitution IV states: "For tools that call IRIS: unit tests MUST use `None` for `iris` parameter and assert `IRIS_UNREACHABLE` error code." But the destructive gate fires at `call_tool` dispatch **before** the impl is reached — the impl itself never receives `iris=None` from a gate-blocked call. T016 is described as testing `iris=None` behavior, but the spec US2 AC2 acceptance criterion is gate blocking (which T017 covers). T016 correctly exercises the no-IRIS path, but it **does not cover AC2** (gate enforcement), which may mislead implementers. The task description conflates two different test concerns.  | Rename T016 to `test_mirror_failover_no_connection_path` and add an explicit note that AC2 (gate enforcement) is covered by T017. Ensure the phase intro does not present T016 as the primary gate test.                                                                                                                    |
| D2  | Constitution Conflict  | CRITICAL | plan.md constitution check II, spec.md FR-003                            | `plan.md` marks constitution check II (ObjectScript Sanity) as PASS and states `SYS.Mirror.JoinMirrorAsAsyncMember` and `SYS.Mirror.BecomePrimary` are "verified against iris-dev-iris — see `research.md`." The spec (FR-003) flags this as "must be verified before coding." The context brief for this analysis also calls out that FR-003 requires live-IRIS verification before implementation tasks. `research.md` confirms both methods exist and provides signatures. **This is resolved**, but it was an open flag in spec.md that was never formally closed. The spec still reads as if verification is pending ("must be verified before coding"), which could mislead anyone treating spec.md as the current state. | Update spec.md FR-003 to note that verification is complete and cite `research.md`. Leaving it as an open action item in a finalized spec will cause confusion during implementation.                                                                                                                                       |
| D3  | Constitution Alignment | HIGH     | tasks.md T025, constitution IX                                           | T025 requires lift ≥ +0.20 for both tools, correctly positioned before Polish. However, the benchmark task JSON in T023/T024 uses `expected_params` with a bare object `{action: ..., ...}` — the constitution's definition of `expected_params` is "the ideal tool call that solves it." The spec provides no guidance on what `success_criteria` should look like for `mirror_failover` when it is blocked by the destructive gate (the benchmark cannot run a live failover in most CI environments). This may cause the lift measurement to be ambiguous or untestable.                                                                                                                                                     | T024 success criterion "response contains `new_role: primary` or structured error" is insufficiently specific for lift measurement. Define one clear success outcome per benchmark task (not an or-condition). Consider two tasks: one for happy-path lift (requires mirror env), one for gate-enforcement lift (does not). |
| E1  | Coverage Gap           | HIGH     | spec.md US1 AC3                                                          | US1 AC3 ("version mismatch named explicitly") has no corresponding test task. T008, T009 cover missing params only. T010 covers gate blocking. T011 covers non-member error on community IRIS. No task tests the version-mismatch error path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Add a unit test task (e.g., T008b) that injects a mock error output containing "version" and asserts `error_code: "MIRROR_VERSION_MISMATCH"`.                                                                                                                                                                               |
| E2  | Coverage Gap           | HIGH     | spec.md US1 AC5                                                          | US1 AC5 (SSL required error) has zero tasks. SSL is accepted as an optional input parameter but no test verifies the error message when `ssl_enabled=false` and the mirror set requires SSL.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Either add a test task or move AC5 to Out of Scope explicitly.                                                                                                                                                                                                                                                              |
| E3  | Coverage Gap           | MEDIUM   | spec.md US2 AC1                                                          | US2 AC1 (happy path: promotion to primary with `success: true` and `new_role: "primary"`) is only covered by T018, which targets the NOT_MIRROR_MEMBER path (community IRIS). The actual happy path is gated on `IRIS_MIRROR_PRIMARY` and described as a "full round-trip" but no task is written for it — only a comment in T011. The `tasks.md` has no explicit `#[ignore]` task for the mirror-set happy path of `mirror_failover`.                                                                                                                                                                                                                                                                                          | Add an explicit `#[ignore]` task for the happy-path round-trip (gated on `IRIS_MIRROR_PRIMARY`). Even if it only runs in special environments, the task should exist so it is not silently omitted.                                                                                                                         |
| E4  | Coverage Gap           | MEDIUM   | constitution (constitution Additional Constraints — Error Code Registry) | New error codes `ALREADY_MEMBER`, `MIRROR_VERSION_MISMATCH`, `ALREADY_PRIMARY`, `NOT_MIRROR_MEMBER` are defined in `data-model.md`. The constitution requires all error codes to be documented in `data-model.md` — this is satisfied. However, `MIRROR_BECOME_PRIMARY_FAILED` (or equivalent) for the `BecomePrimary returned false` path is missing an `error_code` key entirely (see C3).                                                                                                                                                                                                                                                                                                                                    | Add a named error code for the `BecomePrimary returned false` failure path and document it in `data-model.md`.                                                                                                                                                                                                              |
| F1  | Inconsistency          | HIGH     | spec.md FR-006 vs data-model.md, contracts                               | FR-006 says `mirror_failover` takes `confirm: true` as an "extra guard." The contracts and data-model document `confirm` as optional, default `false`, "not currently enforced but reserved." The plan.md implementation pattern for `mirror_failover_impl` takes only `iris` and `client` (no `confirm` parameter at all). Three artifacts disagree on whether `confirm` is part of the function signature.                                                                                                                                                                                                                                                                                                                    | Decide: enforce `confirm` in v1 or remove it from spec/contract/data-model entirely. The current inconsistency will cause an implementation-time decision that should have been made in the spec.                                                                                                                           |
| F2  | Inconsistency          | HIGH     | tasks.md T010 vs T017                                                    | T010 creates a binary test in `test_mcp_binary_config.rs` "or a new `test_mirror_binary.rs`." T017 says "add `#[ignore]` binary test … In the binary test file from T010." If T010 chose the ambiguous option, T017 still references "the binary test file from T010" — if T010 used the new file, T017 works; if T010 appended to `test_mcp_binary_config.rs`, T017 must too. This dependency is implicit and error-prone.                                                                                                                                                                                                                                                                                                     | Resolve B3 (decide the file name in T010) and update T017 to name the file explicitly.                                                                                                                                                                                                                                      |
| F3  | Inconsistency          | MEDIUM   | plan.md impl pattern (mirror_add_async), spec.md FR-001, data-model.md   | The plan.md `iris_mirror_add_async_impl` Rust function signature includes `instance_name: &str` and `async_member_type: u8` as explicit parameters. The spec FR-001 lists `instance_name` as absent from the tool's accepted parameters (FR-001 enumerates: `mirror_name`, `primary_host`, `primary_port`, `ssl_enabled`, `ssl_cert_file`). The data-model adds `instance_name` and `async_member_type`. Three artifacts disagree on what parameters the tool accepts.                                                                                                                                                                                                                                                          | Align FR-001 to include `instance_name` and `async_member_type` as optional parameters (they are documented in data-model.md and plan.md). The spec is the canonical authority and it is missing two parameters.                                                                                                            |
| F4  | Inconsistency          | MEDIUM   | tasks.md branch header                                                   | `tasks.md` header says "working on `102-server-probe`" — this is a stale copy from the tasks template or a different spec. The branch should be `097-mirror-management`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Fix the stale branch name in the tasks.md header. Minor but misleading during code review.                                                                                                                                                                                                                                  |
| F5  | Inconsistency          | MEDIUM   | plan.md Tasks file path                                                  | `plan.md` project structure table shows `tasks.md` path as `specs/097-mirror-management/tasks.md` with the note "(Phase 2 output — not yet created)." tasks.md is now created. This note is stale.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Remove the "not yet created" note from plan.md.                                                                                                                                                                                                                                                                             |
| F6  | Inconsistency          | LOW      | tasks.md notes vs spec.md FR-007                                         | tasks.md Notes say: "The tool description currently mentions `IRIS_ADMIN_TOOLS` for the existing write actions — do not change that wording for existing actions; use `IRIS_WRITE_TOOLS_ENABLED` for `mirror_add_async` description." Spec FR-007 only discusses `IRIS_DESTRUCTIVE_TOOLS_ENABLED` for `mirror_failover`; neither FR-001 nor FR-002 names an env var for `mirror_add_async`. The exact env var name for `mirror_add_async` gate documentation is mentioned only in tasks.md Notes, not in the spec.                                                                                                                                                                                                              | Add an explicit note to spec.md FR-001 or FR-012 stating that the tool description for `mirror_add_async` should reference `IRIS_WRITE_TOOLS_ENABLED`.                                                                                                                                                                      |

---

## Coverage Summary Table

| Requirement Key                  | Has Task? | Task IDs            | Notes                                                                     |
| -------------------------------- | --------- | ------------------- | ------------------------------------------------------------------------- |
| fr-001-mirror-add-async-params   | PARTIAL   | T012, T013, T014    | `instance_name` and `async_member_type` missing from spec FR-001 (see F3) |
| fr-002-impl-function-pattern     | YES       | T012                |                                                                           |
| fr-003-verify-sys-mirror-api     | YES       | T001 (read context) | Verification already in research.md; no explicit close task               |
| fr-004-version-mismatch-error    | NO        | —                   | US1 AC3 has no test task (see E1)                                         |
| fr-005-already-member-preflight  | YES       | T012                | Parsing covered; no dedicated test for ALREADY_MEMBER response            |
| fr-006-failover-confirm-guard    | PARTIAL   | T019                | `confirm` param inconsistently defined (see F1)                           |
| fr-007-failover-destructive-gate | YES       | T004, T017, T019    |                                                                           |
| fr-008-failover-already-primary  | YES       | T019                | No dedicated test for ALREADY_PRIMARY path                                |
| fr-009-failover-not-member       | YES       | T018, T019          |                                                                           |
| fr-010-failover-success-response | PARTIAL   | T019                | Happy-path test exists only in comments (see E3)                          |
| fr-011-tool-description-update   | YES       | T014, T021          |                                                                           |
| fr-012-write-gate-classification | YES       | T004                |                                                                           |
| fr-013-unit-param-validation     | YES       | T008, T009, T016    |                                                                           |
| fr-014-binary-gate-enforcement   | YES       | T010, T017          |                                                                           |
| fr-015-live-iris-non-member      | YES       | T011, T018          |                                                                           |
| us1-ac1-join-success             | PARTIAL   | T012 (impl only)    | No happy-path integration test gated on IRIS_MIRROR_PRIMARY               |
| us1-ac2-already-member-error     | YES       | T012                |                                                                           |
| us1-ac3-version-mismatch-error   | NO        | —                   | No test task (critical gap — see E1, C1)                                  |
| us1-ac4-missing-params-error     | YES       | T008, T009          |                                                                           |
| us1-ac5-ssl-required-error       | NO        | —                   | No task at all (see E2, C2)                                               |
| us2-ac1-promotion-success        | NO        | —                   | No explicit happy-path task (see E3)                                      |
| us2-ac2-gate-blocked             | YES       | T017                |                                                                           |
| us2-ac3-already-primary          | PARTIAL   | T019 (impl only)    | No dedicated test asserts ALREADY_PRIMARY response                        |
| us2-ac4-not-member               | YES       | T018                |                                                                           |

---

## Constitution Alignment Issues

| Principle                 | Status  | Issue                                                                                                                                                                                                                                      |
| ------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| II. ObjectScript Sanity   | WARN    | spec.md FR-003 still reads as an open action item; plan.md marks it PASS. Stale open language in spec causes confusion.                                                                                                                    |
| IV. Test-First            | PARTIAL | T016 is the unit test for `iris_mirror_failover_impl` but tests `IRIS_UNREACHABLE` (no-connection) not gate enforcement. AC2 is covered by T017 (binary test). No spec-mapped test for AC3 (version mismatch) or AC5 (SSL error).          |
| VIII. 90% Coverage Gate   | WARN    | T032 is correctly in Polish phase. However, two acceptance criteria (AC3 version mismatch, AC5 SSL) have no test tasks — their code paths will be uncovered. If the version-mismatch branch is several lines, coverage may drop below 90%. |
| IX. Tool Lift Requirement | PARTIAL | T023–T025 are present and lift phase precedes Polish (correct). T024 success criteria use an or-condition ("response contains `new_role: primary` OR structured error") which is ambiguous for lift scoring (see D3).                      |

---

## Unmapped Tasks

All tasks map to at least one requirement or story. No orphaned tasks found.

---

## Metrics

| Metric                                 | Value                                                              |
| -------------------------------------- | ------------------------------------------------------------------ |
| Total Functional Requirements          | 15 (FR-001 – FR-015)                                               |
| Total User Story Acceptance Criteria   | 9 (US1: 5, US2: 4)                                                 |
| Total Tasks                            | 32 (T001 – T032)                                                   |
| Requirements with ≥1 task (coverage %) | 79% (3 FRs fully uncovered: FR-004, plus US1 AC3, AC5 and US2 AC1) |
| Ambiguity Count                        | 4 (B1–B4)                                                          |
| Duplication Count                      | 2 (A1–A2)                                                          |
| Inconsistency Count                    | 6 (F1–F6)                                                          |
| Constitution Conflicts (CRITICAL)      | 2 (D1, D2)                                                         |
| Total Findings                         | 19                                                                 |
| Critical Issues                        | 2                                                                  |
| High Issues                            | 7                                                                  |
| Medium Issues                          | 7                                                                  |
| Low Issues                             | 3                                                                  |

---

## Next Actions

### Resolve before `/speckit.implement`

**D1 (CRITICAL)** — T016 description conflates `IRIS_UNREACHABLE` path with AC2 gate
enforcement. Rename and add a clarifying note so implementers do not mistake T016 as the
primary gate test for `mirror_failover`.

**D2 (CRITICAL)** — spec.md FR-003 still reads as an open action item ("must be verified
before coding") but research.md is complete. Update spec.md FR-003 to state verification
is done and cite `research.md`.

**F1 (HIGH)** / **F3 (HIGH)** — Three artifacts disagree on `confirm` param behavior and
two params (`instance_name`, `async_member_type`) are absent from spec.md FR-001.
Reconcile before writing a single line of implementation code.

**C1 (HIGH)** / **E1 (HIGH)** — US1 AC3 (version mismatch) has no test. Add a unit test
task before T012. Without it, the `MIRROR_VERSION_MISMATCH` detection code will ship
untested.

**C2 (HIGH)** / **E2 (HIGH)** — US1 AC5 (SSL error path) is an acceptance criterion with
zero coverage. Either add a test task or move it to Out of Scope explicitly.

### Recommended commands

```bash
# After resolving D1, D2, F1, F3, C1, C2 by editing spec.md, plan.md, tasks.md:
# Re-run analyze to confirm issues are closed:
# (from repo root)
# /speckit.analyze   ← re-run this command
```

Then proceed with `/speckit.implement` using the updated tasks.md.

### Safe to defer (LOW/MEDIUM)

- **A1, A2**: Duplication cleanup — harmless during implementation, fix in a follow-up edit.
- **B2**: `confirm` guard ambiguity — resolved if F1 is addressed.
- **B3, F2**: Test file name ambiguity — decide before T010, not a blocker for spec work.
- **C3**: Missing `error_code` on `BecomePrimary returned false` — add to data-model.md.
- **E3**: Happy-path mirror failover test — add a placeholder task even if it can only run
  with `IRIS_MIRROR_PRIMARY` set.
- **F4**: Stale branch name in tasks.md header.
- **F5**: Stale "not yet created" note in plan.md.

---

## Remediation Offer

Would you like me to suggest concrete remediation edits for the top issues (D1, D2, F1,
F3, C1, C2)? I can provide exact diff-ready text for each without modifying any files
until you approve.
