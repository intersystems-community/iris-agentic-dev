# Specification Analysis Report: 101-nopws-connectivity

**Generated**: 2026-09-02
**Artifacts**: spec.md (15 KB), plan.md (4.8 KB), tasks.md (353 lines, T001–T045), constitution.md (v1.3.2)

---

## Findings Table

| ID  | Category           | Severity | Location(s)                                                                   | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Recommendation                                                                                                                                                                                                                                                                                                                                          |
| --- | ------------------ | -------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | ------------------------------------------------------------------------------------------------ |
| C1  | Constitution       | CRITICAL | plan.md:30–46                                                                 | plan.md is an unfilled template. Every cell in the Constitution Check table still reads `PASS / FAIL / N/A` with no actual verdict, and every Technical Context field reads `NEEDS CLARIFICATION`. A plan with "NEEDS CLARIFICATION" in any gate MUST NOT proceed to implementation (constitution §Compliance Review).                                                                                                                                                                                                                                                                                                        | Fill in the plan.md Constitution Check table and Technical Context before implementation begins. Per constitution, any remaining FAIL or NEEDS CLARIFICATION is a hard stop.                                                                                                                                                                            |
| C2  | Constitution       | CRITICAL | plan.md:30–46                                                                 | Constitution IX (Tool Lift Requirement) is unevaluated in plan.md. No `lift-results.md` task exists anywhere in tasks.md. The feature modifies `iris_execute` and `iris_test_server` — both are agent-facing MCP tools. Constitution IX requires a benchmark task in `src/benchmark/tasks/` and lift ≥ +0.20 measured before merge.                                                                                                                                                                                                                                                                                           | Either add a benchmark task and `lift-results.md` recording task to Phase 9 (before polish), or document the N/A justification (internal-only flag) in the spec and get constitution amendment approval.                                                                                                                                                |
| C3  | Constitution       | CRITICAL | tasks.md:209–227 (Phase 8)                                                    | Phase 8 (FR-010, T034–T036) reverses test-first order: T034 is the test, but it comes after the phase header describing what T035/T036 will implement. More critically, T034 is an `#[ignore]` integration test — it tests `docker_only = true` behavior but calls a live tool against IRIS. There is no unit test (Layer 1) for `NOPWS_ATELIER_REQUIRED` that runs without IRIS, violating constitution IV ("unit tests MUST run without a live IRIS connection").                                                                                                                                                           | Add a Layer 1 unit test for `NOPWS_ATELIER_REQUIRED` that calls `iris_doc` with a `None` iris handle and asserts the error code. The integration test (T034) can remain as Layer 3.                                                                                                                                                                     |
| I1  | Inconsistency      | HIGH     | spec.md:229–230 vs data-model.md:74,84 vs contracts/iris_test_server.md:13,23 | Three-way field name conflict for the suggestion field in `iris_test_server` response: spec.md uses `suggestion`, data-model.md uses `nopws_suggestion`, contracts/iris_test_server.md uses `suggestion`. Tasks T028 and T031 use `nopws_suggestion`. The contract is the implementation target — implementers will pick different names and tests will diverge.                                                                                                                                                                                                                                                              | Pick one name before implementation. `nopws_suggestion` is more consistent with the other `nopws_*` sibling fields and is what the tasks use — update spec.md §Key Entities and contracts/iris_test_server.md to match.                                                                                                                                 |
| I2  | Inconsistency      | HIGH     | spec.md:133–134 vs data-model.md vs contracts/                                | `unreachable: true` field in `iris_test_server` response appears in spec.md US4 acceptance criterion 3 ("Then `nopws_detected: false` and `unreachable: true`") but is absent from the data-model field table and from contracts/iris_test_server.md. Tasks T043 (US4 unit test at offset ~158) assert `unreachable: true` response but T031 (implementation) does not set this field.                                                                                                                                                                                                                                        | Add `unreachable: bool` to the data-model and iris_test_server contract, or remove it from the spec acceptance criterion. The spec edge case is the canonical requirement — the data-model is incomplete without it.                                                                                                                                    |
| I3  | Inconsistency      | HIGH     | tasks.md Phase 4 (T022) vs spec.md FR-003 ordering                            | T022 is an implementation task ("Add `nopws_detected` and `nopws_evidence` fields to `iris_test_server` response as placeholders") placed in Phase 4 (User Story 2, iris_execute). These fields belong to User Story 1 (iris_test_server) implemented in Phase 3. Placing them in Phase 4 as placeholders creates a cross-phase ordering where Phase 3 tests (T011 assertion: `"nopws_detected": false`) could pass before T022 runs. Alternatively, T011 cannot pass without T022.                                                                                                                                           | Move T022's placeholder fields to Phase 3 (after T012/T013/T014) where iris_test_server response shape is being built. The note in T022 acknowledges this: "Ensures the response shape is complete for the binary invocation test in T016" — but the binary test in T016 tests `iris_execute`, not `iris_test_server`. The dependency chain is muddled. |
| I4  | Inconsistency      | MEDIUM   | tasks.md T025 vs FR-009 / data-model.md                                       | T025 uses `std::process::Command` (synchronous) to implement SSH docker exec in `IrisConnection`. T020 uses `tokio::process::Command` (async) for the `iris_execute` early-branch by implication (the tool handler is async). Mixed sync/async process spawning in an async context is a correctness issue — `std::process::Command::output()` blocks the async runtime thread.                                                                                                                                                                                                                                               | Use `tokio::process::Command` consistently. The data-model routing logic says "tokio::process::Command" in the comparable iris_execute description. Update T025 to match.                                                                                                                                                                               |
| I5  | Inconsistency      | MEDIUM   | tasks.md T033 vs FR-014 / spec.md                                             | Skill file path conflict: T033 specifies `skills/skills/iris-agentic-dev/nopws-setup/SKILL.md` (deeply nested, two `skills/` components). T032 adds uncertainty: "or `nopws-setup.md`". FR-014 says `skills/nopws-setup.md`. The quickstart.md and spec references all use `skills/nopws-setup.md`. The double-path in T033 (`skills/skills/iris-agentic-dev/nopws-setup/SKILL.md`) looks like a path from a different project's skill convention.                                                                                                                                                                            | Decide the canonical path. If the project stores skills at `skills/*.md` (flat, as FR-014 implies), use `skills/nopws-setup.md`. If the project uses the nested convention, update FR-014 and all spec cross-references. T032 keyword test and T042 `/no-ai-slop` run both need the same path.                                                          |
| I6  | Inconsistency      | MEDIUM   | tasks.md Phase 3 vs Phase 4 user-story numbering                              | tasks.md maps Phase 3 = US1 (NoPWS flag + clear errors) and Phase 4 = US2 (iris_execute fallback). But spec.md lists US1 = NoPWS flag (P1) and US2 = iris_execute fallback (P1) — that matches. However, the MVP section in tasks.md (lines 318–326) says "Phase 3: US1, Phase 4: US2" then lists the implementation strategy as "Phase 3 iris_test_server, Phase 4 iris_execute" — matching spec. The issue is that T022 (adding `nopws_detected`/`nopws_evidence` placeholders) is labeled as US2 work but modifies the US1 response shape.                                                                                 | Relabel T022 as [US1] or split it out explicitly as a cross-story infrastructure task with an explicit dependency note.                                                                                                                                                                                                                                 |
| A1  | Ambiguity          | HIGH     | tasks.md T010 (Phase 3)                                                       | T010 says "construct a tool handler with `nopws = true` config against a closed port" for a Layer 1 unit test. `iris_test_server` makes a live TCP connection — a "closed port" in a unit test context requires either a mock or a port guaranteed closed (e.g., `localhost:1`). The task does not specify how to achieve the closed-port condition without a live connection, leaving the test author to guess.                                                                                                                                                                                                              | Specify the mechanism: use a `WorkspaceConfig` with `host = "127.0.0.1"` and `port = 1` (always closed), or use constitution IV's pattern of `None` for the iris parameter and assert `IRIS_UNREACHABLE`.                                                                                                                                               |
| A2  | Ambiguity          | HIGH     | spec.md FR-005 / tasks.md T029                                                | Auto-detection probes `/usr/irissys/iris.cpf` first, then `/usr/local/etc/irissys/iris.cpf`. T029 uses `cat` + grepping in the caller, while FR-005 says to grep WebServer in the cpf file directly. The key instruction context says to use `grep WebServer <iris_cpf>` (single file at a time). T029 uses `cat <file> 2>/dev/null` and grep in Rust. Either approach works but the exact command matters for edge cases (no grep on Alpine, permission denied without stderr).                                                                                                                                              | Standardize: use `docker exec <container> grep WebServer /usr/irissys/iris.cpf 2>/dev/null` (spec FR-005 phrasing) in T029 and T027 parser test. Remove the `cat`-then-grep variant.                                                                                                                                                                    |
| A3  | Ambiguity          | MEDIUM   | spec.md US2 AC1 / tasks.md T020                                               | The `no_pws` identifier (Rust snake_case for the runtime-detected flag from `derive_capabilities()`) versus `nopws` (the toml field and config bool) is never formally defined in the spec or data-model. T020 reads `(docker_only, no_pws, ssh_host)` from `ConnectionState` — but `ConnectionState` currently has `no_pws` (derived from version detection), while the new TOML field is `nopws: bool` in `WorkspaceConfig`. The implementation must merge both signals. Neither the spec nor the data-model explains how these combine.                                                                                    | Add a section to data-model.md (or spec §Key Entities) defining the combination rule: `effective_no_pws = config.nopws                                                                                                                                                                                                                                  |                                                                                                                                                                                                                                                                                                                                                              | connection.no_pws`. Document which field to read in `ConnectionState`vs`WorkspaceConfig`. |
| A4  | Ambiguity          | MEDIUM   | tasks.md T013 vs T014                                                         | T013 says "set message field in the response" when `nopws = true` and web probe fails. T014 says "ensure `iris_test_server` returns early without any HTTP attempt" when both `nopws = true` AND `docker_only = true`. The distinction between "nopws alone" vs "nopws + docker_only" in `iris_test_server` behavior is not in the spec or contracts — spec only says suppress the error, not change execution flow. This creates a sub-behavior untested by any acceptance criterion.                                                                                                                                        | Either add an explicit acceptance criterion in the spec for the `nopws = true + docker_only = true` combo in `iris_test_server`, or remove T014 and let T013 cover both cases.                                                                                                                                                                          |
| U1  | Underspecification | HIGH     | tasks.md T010 / Constitution IV                                               | T010 is labeled as a Layer 1 unit test but it tests `iris_test_server` behavior with "a closed port." Constitution IV requires unit tests to "run without a live IRIS connection" and use `None` for the `iris` parameter to assert `IRIS_UNREACHABLE`. T010 does not follow this pattern — it requires a TCP connection attempt (even to `localhost:1`). There is no test that asserts `IRIS_UNREACHABLE` for the NoPWS path without any connection.                                                                                                                                                                         | Separate T010 into: (a) a pure config test (`nopws = true` round-trip in WorkspaceConfig, which T004 already covers); (b) the closed-port behavior test which belongs in the integration layer (`#[ignore]`), not unit. Or document how the unit test achieves no-connection behavior (port=1 trick).                                                   |
| U2  | Underspecification | HIGH     | spec.md FR-003 / tasks.md                                                     | FR-003 specifies 4 new fields for `iris_test_server`. The data-model adds a 5th (`nopws_suggestion`). Neither `nopws_suggestion` nor the `unreachable` field (US4 AC3) has an explicit FR entry. Two response fields are being added without functional requirement backing.                                                                                                                                                                                                                                                                                                                                                  | Add FR-003a for `nopws_suggestion` and FR-003b for `unreachable` (or fold them into FR-003 with a parenthetical), or remove them from the acceptance criteria. Untracked fields are constitution V violations in reverse — callers can't rely on fields that aren't specified.                                                                          |
| U3  | Underspecification | MEDIUM   | spec.md / tasks.md Phase 5                                                    | Phase 5 (SSH routing) has no Layer 3 live IRIS integration test. Phase 3 gate requires T022 (live IRIS), Phase 4 gate requires T028 (live IRIS), but Phase 5 gate only requires T023 (unit). SSH routing to a real host is never tested end-to-end in this spec. The task notes this as "best-effort" but the spec's SC-002 implies execution must succeed, not just compile.                                                                                                                                                                                                                                                 | Add a `#[ignore]` integration test that uses `ssh_host = "localhost"` (loopback) + local Docker as a proxy for the SSH path. T037 in the alternative plan attempted this with caveats — add it back as a gated but acknowledged-optional Layer 3 test.                                                                                                  |
| U4  | Underspecification | MEDIUM   | tasks.md Phase 8 (T035)                                                       | T035 checks `docker_only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |                                                                                                                                                                                                                                                                                                                                                         | no_pws`AND web port not available before returning`NOPWS_ATELIER_REQUIRED`. The condition "web port not available" requires an active HTTP probe in the early check, which contradicts "no HTTP attempt" semantics — the early check IS an HTTP attempt. The contract says "no HTTP attempt," implying the check should be purely config-based (`docker_only |                                                                                           | no_pws` without probing). | Decide: is the `NOPWS_ATELIER_REQUIRED` guard config-based (no probe) or probe-based? The spec says "Tools requiring Atelier by design… return a clear message." This should be config-only: if `docker_only |     | nopws`, return the error immediately without probing. Update T035 to remove the probe condition. |
| U5  | Underspecification | LOW      | spec.md §Assumptions                                                          | The 10-second docker exec timeout (from spec §Assumptions) is not reflected in any task as an explicit implementation requirement. T029 mentions "10-second timeout per docker exec call" but T020/T021 (iris_execute docker exec path) does not mention timeout behavior. If the docker exec path hangs, `iris_execute` blocks indefinitely.                                                                                                                                                                                                                                                                                 | Add timeout behavior to T020 or T021: "if docker exec returns no output within 10 seconds, return a timeout error with the container name."                                                                                                                                                                                                             |
| E1  | Coverage Gap       | HIGH     | FR-011 / SC-005                                                               | FR-011 (TOML round-trip test) is the constitution-mandated silent-drop test. T004 covers it well. However, there is no test asserting that adding `nopws: false` (the default) to an existing config that omits the field does NOT break parsing — i.e., an explicit `assert!(cfg.nopws == false)` for a config that predates the new field. T004 tests `nopws_default_is_false` by parsing only `container = "x"` but this is tested as part of T004, which needs to FAIL first. The test name suggests it runs pre-implementation and must fail. If `#[serde(default)]` is present from the start, it may pass immediately. | This is low-risk given `#[serde(default)]` but verify: the round-trip test's failure mode is "field not in struct" (compile error), not "field defaults wrong." Document the expected FAIL reason explicitly in T004.                                                                                                                                   |
| E2  | Coverage Gap       | HIGH     | Constitution IX / tasks.md                                                    | No benchmark task and no `lift-results.md` task exists. `iris_execute` is the primary agent-facing tool being modified — it has baseline behavior that can be benchmarked (agent success rate on NoPWS container tasks). Constitution IX is not marked N/A in plan.md (plan.md has no verdicts). This is both a constitution violation (C2) and a coverage gap.                                                                                                                                                                                                                                                               | See C2. Either add a benchmark task in Phase 9 or formally document N/A justification in spec and plan.                                                                                                                                                                                                                                                 |
| E3  | Coverage Gap       | MEDIUM   | FR-012 (binary invocation test) / tasks.md                                    | FR-012 requires a binary invocation test that asserts `nopws` field present in `iris_test_server` response. T016 covers `iris_execute` binary invocation. T011 covers `iris_test_server` live IRIS. There is no binary invocation test (`#[ignore]`, IAD_BINARY) for `iris_test_server` itself — no Layer 2 test for that tool. FR-012 specifically names `iris_test_server`.                                                                                                                                                                                                                                                 | Add a Layer 2 binary invocation test for `iris_test_server`: spawn `IAD_BINARY`, call `tools/call iris_test_server`, assert response JSON contains `nopws`, `web_available`, `nopws_detected` keys. This directly satisfies FR-012.                                                                                                                     |
| E4  | Coverage Gap       | MEDIUM   | SC-003 / tasks.md T032                                                        | SC-003 requires "a reviewer reading the skill and confirming the steps are complete." T032 (the automated keyword test) only verifies file existence and keywords — it cannot verify logical completeness of the skill flow. There is no task that explicitly documents the human review step as a gate. The Phase 7 gate says "human reviewer confirms steps are complete" but this is not a trackable task.                                                                                                                                                                                                                 | Add a task in Phase 7 like: "T032b: Human review of SKILL.md — confirm detection → option A → option B → first-boot flow is complete and correct. Gate: reviewer sign-off before Phase 7 is marked complete."                                                                                                                                           |
| D1  | Duplication        | MEDIUM   | tasks.md T011 vs T028                                                         | T011 (Phase 3) and T028 (Phase 6) both assert `iris_test_server` against `iris-dev-iris` returns `nopws_detected: false`. T028 explicitly acknowledges this: "This extends the test from T011 with the specific `nopws_detected` assertion." They can share a test function — T028 should augment T011's assertion rather than add a new test. Running the same live IRIS call twice in the same test file adds test time without adding coverage.                                                                                                                                                                            | Make T028 an assertion added to the existing T011 test function, not a new test. Remove the `[P]` parallel marker since it depends on T011.                                                                                                                                                                                                             |
| D2  | Duplication        | LOW      | spec.md §User Scenarios vs §Requirements                                      | Several acceptance criteria in US1–US5 are near-verbatim restatements of FR-001 through FR-015. This is normal spec structure, but FR-003 and FR-004 overlap with US1 AC1 and AC3 almost word-for-word. No action required — duplication is structural, not a bug.                                                                                                                                                                                                                                                                                                                                                            | No change needed. Noted for completeness.                                                                                                                                                                                                                                                                                                               |

---

## Coverage Summary Table

| Requirement Key                                  | Has Task? | Task IDs                           | Notes                                                                 |
| ------------------------------------------------ | --------- | ---------------------------------- | --------------------------------------------------------------------- |
| FR-001 (nopws: bool in WorkspaceConfig)          | Yes       | T004, T005, T006, T009             | Well covered: write-fail-implement-pass pattern                       |
| FR-002 (ssh_host in WorkspaceConfig)             | Yes       | T004, T007, T008, T009             | TOML round-trip covers ssh_host                                       |
| FR-003 (iris_test_server new fields)             | Partial   | T010, T011, T012                   | Missing `nopws_suggestion` and `unreachable` fields per I1/I2         |
| FR-004 (structured NoPWS error)                  | Yes       | T012, T013                         | Covered                                                               |
| FR-005 (auto-detection: iris.cpf + superserver)  | Yes       | T027, T029, T030, T031             | Covered; see A2 for command form ambiguity                            |
| FR-006 (auto-detection → paste snippet)          | Yes       | T031                               | Covered                                                               |
| FR-007 (iris_execute docker exec fallback)       | Yes       | T015, T016, T017, T018, T019, T020 | Covered                                                               |
| FR-008 (execution_path in iris_execute response) | Yes       | T015, T016, T017, T018, T020       | Covered                                                               |
| FR-009 (SSH routing via ssh_host)                | Yes       | T023, T024, T025, T026             | No Layer 3 live SSH test — see U3                                     |
| FR-010 (Atelier-required tool guard)             | Partial   | T034, T035, T036                   | Missing Layer 1 unit test — see C3; T034 condition ambiguity — see U4 |
| FR-011 (TOML round-trip unit test)               | Yes       | T004                               | Well specified                                                        |
| FR-012 (binary invocation test)                  | Partial   | T016                               | Covers iris_execute; missing iris_test_server binary test — see E3    |
| FR-013 (live IRIS integration test)              | Yes       | T017, T028                         | Covered                                                               |
| FR-014 (skills/nopws-setup.md)                   | Yes       | T032, T033                         | Skill path ambiguity — see I5                                         |
| FR-015 (skill description keywords)              | Yes       | T032                               | Keyword test covers this                                              |
| SC-001 (plain language, no raw conn refused)     | Yes       | T013                               | Covered                                                               |
| SC-002 (iris_execute → docker exec → result)     | Yes       | T017                               | Covered                                                               |
| SC-003 (agent reads skill → working config)      | Partial   | T032, T033                         | Automated test only; no human review task — see E4                    |
| SC-004 (existing Atelier tests unchanged)        | Yes       | T039                               | Full test suite pass covers regression                                |
| SC-005 (TOML round-trip passes)                  | Yes       | T004, T009                         | Covered                                                               |

---

## Constitution Alignment Issues

| Principle                      | Status              | Finding                                                                                                                                                                                                                           |
| ------------------------------ | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| I. Zero-Install Binary         | Unverified          | plan.md not filled in; T025 uses `std::process::Command` (acceptable — no new crates). On net: likely PASS, but unverifiable until plan.md is completed.                                                                          |
| II. ObjectScript Sanity        | Unverified          | plan.md not filled in. The `ChangePassword` ObjectScript in the skill and iris.cpf grep command via `docker exec` do not call IRIS APIs. No new IRIS ObjectScript methods introduced — likely N/A, but must be stated explicitly. |
| III. HTTP-First Execution      | PASS with condition | docker exec is correctly positioned as fallback (early-branch only when `docker_only                                                                                                                                              |     | nopws`). HTTP-fail → docker fallback (T018/T019) is additive. No Merged-tier tool is being made Docker-required. Constitution III satisfied. |
| IV. Test-First, Fixture-Driven | PARTIAL FAIL        | T034 (FR-010, Phase 8) uses a live `#[ignore]` integration test as the sole test for `NOPWS_ATELIER_REQUIRED`. No Layer 1 unit test. See C3.                                                                                      |
| V. Output Shape Parity         | PASS                | `execution_path` is additive; `method` retained for backward compat. Contract in `contracts/iris_execute_101.md` documents both fields.                                                                                           |
| VI. Environment Guard          | N/A                 | No write-capable tools added or modified.                                                                                                                                                                                         |
| VII. Dependency Minimalism     | Unverified          | plan.md not filled in. No new crates identified in tasks. Likely PASS.                                                                                                                                                            |
| VIII. 90% Coverage Gate        | PASS                | T041 is the coverage gate task in Phase 9. Command is correct (`--include-ignored`).                                                                                                                                              |
| IX. Tool Lift Requirement      | FAIL                | No benchmark task, no lift-results.md, no phase before Polish for lift evidence. See C2.                                                                                                                                          |
| X. ObjectScript Coverage       | N/A                 | Pure Rust feature — no new ObjectScript.                                                                                                                                                                                          |

---

## Unmapped Tasks

All tasks T001–T045 map to at least one requirement or success criterion. No unmapped tasks found.

---

## Metrics

- **Total Requirements**: 15 functional (FR-001 through FR-015) + 5 success criteria (SC-001 through SC-005) = 20
- **Total Tasks**: 45 (T001–T045)
- **Coverage %**: 85% (17 of 20 requirements have task coverage; FR-003, FR-012, SC-003 are partial — see E3, E4, I1, I2)
- **Ambiguity Count**: 5 (A1–A4, A3)
- **Duplication Count**: 2 (D1–D2)
- **Critical Issues Count**: 3 (C1–C3)

---

## Next Actions

### CRITICAL — Resolve Before /speckit.implement

1. **C1 — Fill plan.md** before any implementation begins. The unfilled constitution check table is a hard stop per constitution §Compliance Review. Run `/speckit.plan` against the feature or manually fill the Technical Context and Constitution Check table in `specs/101-nopws-connectivity/plan.md`.

2. **C2 — Constitution IX Lift Requirement** — Either add a benchmark task to Phase 9 with a `lift-results.md` recording task, or document in spec.md §Out of Scope why this qualifies for the internal-tool N/A exception and add the explicit justification to plan.md's Constitution Check row IX.

3. **C3 — Add Layer 1 unit test for Phase 8 (FR-010)** — Add a task before T035 that writes a unit test asserting `NOPWS_ATELIER_REQUIRED` without IRIS (call `iris_doc` with `None` iris handle and `docker_only = true` config). Then move T034 to be the Layer 3 integration test (relabeled accordingly).

### HIGH — Resolve Before Implementation of Affected Phase

4. **I1 — Suggestion field name** — Pick `nopws_suggestion` or `suggestion` and update spec.md §Key Entities + `contracts/iris_test_server.md`. Do this before Phase 3/6 implementation.

5. **I2 — `unreachable` field** — Add to data-model and contract or remove from spec US4 AC3. Do this before Phase 6 implementation.

6. **I3 — T022 placement** — Move `nopws_detected`/`nopws_evidence` placeholder fields to Phase 3 (alongside T012/T013/T014). Resolve before Phase 3 implementation.

7. **A3 — `no_pws` vs `nopws` merge semantics** — Document the combination rule (`effective_no_pws = config.nopws || connection.no_pws`) in data-model.md before Phase 4 (T020) implementation. This affects the `ConnectionState` read in T020.

8. **E3 — Missing binary invocation test for `iris_test_server`** (FR-012) — Add Layer 2 test task before Phase 3 gate.

### MEDIUM — Address Before Phase Affected

9. **I4 — tokio vs std process** — Update T025 to use `tokio::process::Command`. Do before Phase 5 implementation.

10. **I5 — Skill file path** — Resolve `skills/nopws-setup.md` vs `skills/skills/iris-agentic-dev/nopws-setup/SKILL.md` before Phase 7. Check `skills/` directory structure in the repo.

11. **U4 — Phase 8 probe semantics** — Clarify whether `NOPWS_ATELIER_REQUIRED` is config-only or probe-based. Update T035 before Phase 8 implementation.

12. **U3 — SSH Layer 3 test gap** — Consider adding a loopback SSH integration test in Phase 5.

### LOW — Can Be Addressed Anytime

13. **D1 — Merge T011/T028** — Consolidate duplicate `iris_test_server` live IRIS calls.

14. **U5 — Docker exec timeout in iris_execute** — Add explicit timeout behavior note to T020/T021.

---

## Remediation Offer

Would you like concrete remediation edits for the top issues? Priority suggestions:

- **C1**: Write the filled plan.md sections (Technical Context + Constitution Check verdicts)
- **C2**: Draft the Constitution IX lift-results task wording for Phase 9, or the N/A justification
- **C3**: Write the Layer 1 unit test task text for FR-010
- **I1**: Propose the canonical field name and identify all locations that need updating
- **I2**: Draft the `unreachable` field addition to data-model.md and contracts/iris_test_server.md
